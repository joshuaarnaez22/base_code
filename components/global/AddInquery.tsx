import React from 'react';

interface Props {
  isOpen: any;
  onClose: any;
  type?: string;
}
const AddInquery = ({ isOpen, onClose, type }: Props) => {
  return <div>AddInquery</div>;
};

export default AddInquery;
